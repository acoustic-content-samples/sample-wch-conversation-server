#!/usr/bin/env node
const debug = require('debug')('wchbotserver:www');
const app = require('../app');
const creds = app.get('appEnv').credentials;

var server = app.listen(creds.port, creds.bind, function() {
  debug(`Server started at ${creds.bind}:${creds.port}`);
});
